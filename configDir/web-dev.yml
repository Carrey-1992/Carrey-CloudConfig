spring:
  profiles:
    include: dev-eureka,dev-logback,dev-rabbitMq
  #spring.cloud.bus相关配置
  cloud:
    bus:
      enabled: true
      #开启消息跟踪
      trace:
        enabled: true
  zipkin:
    base-url: http://localhost:9411
name: web-ddd
